services:
  db:
    image: mongodb/mongodb-community-server
    restart: always
    ports:
      - 27017:27017
    environment:
      MONGO_DATABASE: nest-pokemon
      
    volumes:
      - ./mongodb:/data/db